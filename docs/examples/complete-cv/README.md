# Complete Markdown CV

This deliberately long CV exercises the complete Markdown document model. It
contains multiple entries in every standard section, custom sections, rich
text, links, Unicode, varied date precision, and enough content to test page
breaks in PDF output.

Check and build it from this directory:

```sh
cac check
cac build --format pdf,html
```

Generated artifacts are written to `offering/` and ignored by Git.
